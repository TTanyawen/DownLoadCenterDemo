spring:
  datasource:
    url: jdbc:mysql://localhost:3306/forstudy?serverTimezone=Asia/Shanghai
    username: root
    password: 2640
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate.dialect: org.hibernate.dialect.MySQL5InnoDBDialect
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
    listener:
      simple:
        concurrency: 1
        max-concurrency: 10
    template:
      mandatory: true

server:
  port: 8087


    # 开redis服务
    # 开rabbit服务
    # net start rabbitMQ
  # 关 net stop rabbitMQ

